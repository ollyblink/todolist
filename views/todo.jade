//
   Created by olzi on 11.08.2016.

extends layout

block content
    .container
        if (user)
            h1 Enter a new To Do
            br
            form(role='form', action="/todo/add",method="post", style='max-width: 300px;')
                .form-group
                    input.form-control(type='text', name="title", placeholder='Enter title')
                .form-group
                    input.form-control(type='textarea', name="description", placeholder='Enter description')
                .form-group
                    input.form-control(type='textarea', name="description", placeholder='Enter description')
                button.btn.btn-default(type='submit') Submit
            &nbsp;
            a(href='/')
            button.btn.btn-primary(type="button") Cancel

